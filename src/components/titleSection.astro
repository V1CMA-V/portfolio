---
interface Props {
  title: string
}

const { title } = Astro.props
---

<div class="flex flex-col gap-4 text-center mb-8">
  <h2 class="text-4xl font-bold shadow-text">{title}</h2>
  <p class="text-lg text-text-secondary"><slot /></p>
</div>
